import asyncio
import websockets
import json
import uuid

async def test():
    # Attempt to connect to a random session UUID. We need a real session however...
    # Well, we just want to see if the websocket server throws an error when processing.
    pass

asyncio.run(test())
